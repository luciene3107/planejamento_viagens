openapi: 3.0.3
info:
  title: Planejamento Viagens API
  version: 1.0.0
  description: Estrutura inicial da API REST com JWT e MongoDB.
servers:
  - url: http://localhost:3000
tags:
  - name: Health
  - name: Heuristicas
  - name: Usuarios
  - name: Auth
paths:
  /api/health:
    get:
      tags: [Health]
      summary: Health check público
      responses:
        "200":
          description: API operacional
  /api/protected/health:
    get:
      tags: [Health]
      summary: Health check protegido com JWT
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Token válido
        "401":
          description: Token inválido ou ausente
  /api/heuristicas/{id}:
    get:
      tags: [Heuristicas]
      summary: Consultar heuristica por id
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
          example: "6814f42db637f17f43549a76"
      responses:
        "200":
          description: Heuristica encontrada
  /api/heuristicas:
    post:
      tags: [Heuristicas]
      summary: Cadastrar heuristica de viagem
      security:
        - bearerAuth: []
  /api/usuarios:
    post:
      tags: [Usuarios]
      summary: Registrar novo usuario
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateHeuristicaRequest"
      responses:
        "201":
          description: Heuristica criada com sucesso
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HeuristicaResponse"
        "400":
          description: Dados invalidos
        "401":
          description: Autenticacao obrigatoria
        "409":
          description: Nome da heuristica duplicado para o usuario
              $ref: "#/components/schemas/RegisterRequest"
      responses:
        "201":
          description: Usuario criado com sucesso
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserCreatedResponse"
        "400":
          description: Dados obrigatorios ausentes ou senha fora do criterio minimo
        "409":
          description: Email ja utilizado
  /api/auth/register:
    post:
      tags: [Auth]
      summary: Registrar novo usuário
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegisterRequest"
      responses:
        "201":
          description: Usuário criado com sucesso
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthResponse"
        "409":
          description: Email já utilizado
  /api/auth/login:
    post:
      tags: [Auth]
      summary: Autenticar usuário e retornar JWT
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginRequest"
      responses:
        "200":
          description: Login realizado com sucesso
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthResponse"
        "401":
          description: Credenciais inválidas
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  schemas:
    RegisterRequest:
      type: object
      required: [name, email, password]
      properties:
        name:
          type: string
          example: Luciene
        email:
          type: string
          format: email
          example: luciene@email.com
        password:
          type: string
          format: password
          example: "123456"
    LoginRequest:
      type: object
      required: [email, password]
      properties:
        email:
          type: string
          format: email
          example: luciene@email.com
        password:
          type: string
          format: password
          example: "123456"
    CreateHeuristicaRequest:
      type: object
      required: [nome, descricao]
      properties:
        nome:
          type: string
          example: Menor custo
        descricao:
          type: string
          example: Prioriza opcoes com menor custo total.
    HeuristicaResponse:
      type: object
      properties:
        heuristica:
    UserCreatedResponse:
      type: object
      properties:
        user:
          type: object
          properties:
            id:
              type: string
              example: "6814f42db637f17f43549a76"
            nome:
              type: string
              example: Menor custo
            descricao:
              type: string
              example: Prioriza opcoes com menor custo total.
            status:
              type: string
              example: ativa
            usuario:
              type: string
              example: "507f1f77bcf86cd799439011"
            name:
              type: string
              example: Luciene
            email:
              type: string
              format: email
              example: luciene@email.com
    AuthResponse:
      type: object
      properties:
        token:
          type: string
          example: eyJhbGciOi...
        user:
          type: object
          properties:
            id:
              type: string
              example: "6814f42db637f17f43549a76"
            name:
              type: string
              example: Luciene
            email:
              type: string
              format: email
              example: luciene@email.com
